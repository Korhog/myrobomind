<Page
    x:Class="rMind.DriverForge.Views.Drivers.DriverViewInfo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:rMind.DriverForge.Views.Drivers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0, 0, 0, 4"/>
        </Style>
    </Page.Resources>

    <Grid>
        <StackPanel Width="250" Margin="10" HorizontalAlignment="Left">
            <Button Content="Save"/>

            <TextBlock Text="Name" Margin="0, 8, 0, 4"/>
            <TextBox Text="{Binding Name}"/>

            <TextBlock Text="Semantic Name"/>
            <TextBox Text="{Binding SemanticName}"/>
        </StackPanel>

    </Grid>
</Page>
