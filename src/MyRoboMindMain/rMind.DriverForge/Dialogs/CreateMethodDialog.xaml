<ContentDialog
    x:Class="rMind.DriverForge.Dialogs.CreateMethodDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:rMind.DriverForge.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Создать метод"
    PrimaryButtonText="Create"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">
    <Grid>
        <StackPanel>
            <StackPanel.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="Gray"/>
                </Style>                
            </StackPanel.Resources>

            <TextBlock Text="Имя метода"/>
            <TextBox/>

            <TextBlock Text="Семантическое имя метода"/>
            <TextBox/>

            <MenuFlyoutSeparator/>

            <RadioButton GroupName="One" Content="Node based" IsChecked="True" />
            <TextBlock Text="Метод основанный на соединении блоков"/>
            <RadioButton GroupName="One" Content="Script"/>
            <TextBlock Text="Метод написанный на языке C или Assembler"/>
        </StackPanel>  
    </Grid>
</ContentDialog>
