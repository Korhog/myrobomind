<Page
    x:Class="MyRoboMind.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyRoboMind"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"    
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Background="{ThemeResource SystemAccentColorDark2}">            
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Height" Value="50"/>
                </Style>
            </StackPanel.Resources>
            <Button VerticalAlignment="Top" Click="Button_Subscribe">
                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE17B;" />
            </Button>
            <Button VerticalAlignment="Top" Click="Button_Unsubscribe">
                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE17A;" />                
            </Button>
            <Button VerticalAlignment="Top" Click="Button_Click">
                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE964;" />
            </Button>
            <Button VerticalAlignment="Top" Click="Button_B_Click">
                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE950;" />
            </Button>
        </StackPanel>

        <ScrollViewer
                Grid.Column="1"
                ZoomMode="Enabled"              
                VerticalScrollBarVisibility="Visible" 
                HorizontalScrollBarVisibility="Visible" 
                IsHorizontalRailEnabled="False"
                IsVerticalRailEnabled="False"
                x:Name="scroll" >
            <Canvas 
                    Width="10000"
                    Height="6000"
                    x:Name="canvas" 
                    Background="Transparent" 
                    UseLayoutRounding="True">
            </Canvas>
        </ScrollViewer>

    </Grid>
</Page>
